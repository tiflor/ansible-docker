---
# defaults file for ansible-docker

docker_apt_ppa: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"


# docker Compose
docker_compose_version: 1.23.1